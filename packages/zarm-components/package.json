{
  "name": "@zarm-design/components",
  "version": "0.0.2",
  "description": "",
  "module": "es/index.js",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "build:lib": "rimraf lib && zarm build --mode lib --path src --out-dir lib --copy-files && tsc --outDir lib -d --emitDeclarationOnly",
    "build:es": "rimraf es && zarm build --mode es --path src --out-dir es --copy-files",
    "build": "npm run build:lib && npm run build:es"
  },
  "author": "jerome.lin@zhongan.com",
  "license": "MIT",
  "peerDependencies": {
    "react": ">=16.3.1",
    "react-dom": ">=16.3.1"
  },
  "devDependencies": {
    "@types/react": "^16.8.6",
    "@types/react-dom": "^16.9.0",
    "@zarm-design/cli": "^0.0.25",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "rimraf": "^3.0.2"
  }
}
